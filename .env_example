# The host on which to run the server.
SERVER_HOST=localhost

# The port on which to run the server.
SERVER_PORT=3000
API_HOST=localhost
API_PORT=2121
API_PREFIX=/api/v1

# The port on which to run the client bundle dev server (i.e. used during
# development only).
CLIENT_DEVSERVER_PORT=3001

MONGO_URI=mongodb://localhost:27017/imgs
RAVEN_KEY=
RAVEN_SECRET=